include hot-list-item

mixin hot(data)
  section.main-page__hot.hot
    h2.hot__name Самое обсуждаемое
      span.hot__icon.hot__icon--fire
    if data.length
      ul.hot__list
        each item in data
          +hot-list-item(item)
    else
      p.hot__empty Здесь пока ничего нет...